// Local/dev placeholder — intentionally sets nothing, so database.html,
// standard.html, and admin.html fall back to their built-in
// `http://localhost:8001` default. The production deploy workflow
// (.github/workflows/deploy.yml) overwrites this file with the real API URL
// before uploading — see DEPLOYMENT.md. Do not hardcode a production URL here.
// window.WATERLINE_API_BASE = 'https://api.example.com';
